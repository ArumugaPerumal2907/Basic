streamlit>=1.0.0
pandas>=1.5.0
pypdf>=3.0.0
openai>=1.0.0
numpy>=1.20.0
python-dotenv>=0.19.0

# Optional: enables the NER layer (names/addresses detection)
spacy>=3.0.0

# New: multi-format document support
python-docx>=0.8.0
openpyxl>=3.0.0

# New: local OCR fallback for images
pytesseract>=0.3.0
Pillow>=8.0.0

# New: data warehouse export
pyarrow>=6.0.0
scikit-learn>=1.0.0

# Optional: OCR via Sarvam Vision (Document Intelligence API)
# To enable Sarvam Vision OCR for scanned PDFs and images with
# Indian-language support, install the SDK separately:
#   pip install git+https://github.com/sarvamai/python-sdk.git
# 
# Without sarvamai, plain PNG/JPG images still get local OCR via
# pytesseract (English only), and scanned PDFs just return any extractable
# text. This graceful degradation keeps the app usable out of the box.
